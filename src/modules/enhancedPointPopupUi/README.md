# Улучшенный UI попапа точки (enhancedPointPopupUi)

Косметические правки нативного попапа точки: крупные игровые кнопки, мелкий текст метаданных, подкраска активной звезды и замка.

## Возможности для игрока

- **Крупные игровые кнопки.** Нативные кнопки в `.i-buttons` (deploy / repair / draw / discover) растянуты до `min-height: 72px` для удобного попадания пальцем на мобильном.
- **Уменьшенный текст метаданных.** Строки в `.i-stat__entry` (расстояние, владелец, линии, регионы) сжаты до `0.7rem` - крупный текст для каждой строки занимает много места и оттесняет деплой-слайдер вниз. Строка `.i-stat__cores` исключена: ей нужен полный размер для читаемости уровней ядер.
- **Подкраска активной звезды и замка.** Нативные `.i-flag-btn` в `.i-stat__tools` переключают только иконку (filled vs outline), но цвет оставляют серым. Активная звезда подкрашивается в `var(--level-1)` (золото), активный замок - в `var(--ol-brand-color)` (синий). Цвета те же, что были у удалённой подсистемы `pointMarkButtons`. Активное состояние определяется по `href` у `<use>`: `#fas-star` / `#fas-lock`.

## Архитектура

`enable()` инжектит `styles.css` через `injectStyles`, `disable()` снимает через `removeStyles`. Никаких DOM-инжектов, MutationObserver и runtime-обёрток - чистый CSS.

## Файловая структура

| Файл                           | Назначение                                                                   |
| ------------------------------ | ---------------------------------------------------------------------------- |
| `enhancedPointPopupUi.ts`      | Определение модуля + lifecycle install/uninstall                             |
| `enhancedPointPopupUi.test.ts` | Тесты на инжект стилей и изоляцию селекторов от нативных блоков попапа точки |
| `styles.css`                   | Стили крупных игровых кнопок и мелкого текста метаданных                     |

## Настройки

Модуль не имеет настроек. Включён по умолчанию.
