Создать новый модуль `$ARGUMENTS` (например: `/new-module keepScreenOn`).

**Прочитай и выполни [docs/agent/new-module.md](../../docs/agent/new-module.md).** Подставь имя модуля из `$ARGUMENTS` или из сообщения пользователя согласно workflow.
